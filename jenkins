pipeline {
    agent any
    
    stages {
        
        stage('github-clone-backend') {
            steps {
                git branch: 'main', credentialsId: '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', url: 'https://github.com/iam6ukk/qstudy.git'
            }
        }

        stage('github-clone-frontend') {
            steps {
                git branch: 'main', credentialsId: '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', url: 'https://github.com/iam6ukk/qstudy_front.git'
            }
        }

        stage('Frontend Building') {
            steps {
                sh '''
                    ls -l
                    cd qstudy_front
                    ls -l

                    npm run build
                '''
            }
        }

        stage('move front build files') {
            steps {
                sh '''
                    cp -r build/* ../qstudy/src/main/resources/static
                '''
            }
        }

        stage('Backend Building') {
            steps {
                // Maven 또는 Gradle을 사용하여 프로젝트 빌드
                sh '''
                    ls -l
                '''
            }
        }
   	}
}
